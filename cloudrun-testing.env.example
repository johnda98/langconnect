# Fill this file with testing values, then copy to cloudrun-testing.env (which is gitignored).
#
# Supabase (test project)
OPENAI_API_KEY=<copy-from-.env>
SUPABASE_URL=<copy-from-.env>
SUPABASE_KEY=<copy-from-.env>

# Testing Cloud SQL (clone)
POSTGRES_HOST=/cloudsql/your-vehicle-ai-staging:us-central1:langconnect-testing-sql
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=Agentbear0011!
POSTGRES_DB=langconnect_test

# CORS for local webapp only
ALLOW_ORIGINS=["http://localhost:3000"]
